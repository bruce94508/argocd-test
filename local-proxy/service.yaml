apiVersion: v1
kind: Service
metadata:
  name: local-proxy
  namespace: apts-racev
spec:
  type: NodePort
  selector:
    app: local-proxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
